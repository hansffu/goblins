#+title: Goblins: live package grants from an interactive Fish shell

* Run it in two terminals

Build once from the repository root:

#+begin_src sh
nix --extra-experimental-features 'nix-command flakes' build \
  --out-link result-goblins path:.#goblins
#+end_src

Put =result-goblins/bin= on PATH in both host terminals, or use
=./result-goblins/bin/goblins= directly. =nix develop path:.= also supplies
=goblins=. No service or global installation is required.

In the upper terminal:

#+begin_src sh
goblins serve
#+end_src

In the lower terminal:

#+begin_src sh
goblins run shell
#+end_src

The lower terminal is a real interactive Fish shell. It starts with Fish and
=agent-sandbox.nix='s =commonTools=: coreutils, curl, which, Git, ripgrep, fd,
sed, grep, find, diff, less, awk, and jq. Editing, completion, Ctrl-C, terminal
applications, and resize use its PTY directly. The upper terminal owns the
session and presents package approvals; shell output never becomes approval
input.

The shell preset starts Fish with a private HOME. To load host Fish settings,
add =roDirs = [ "$HOME/.config/fish" ];= to a custom =mkGoblin= configuration.
That directory must exist on the host running =serve=. Tools and other paths
referenced by the config need their own package or startup grants.

Inside Fish, try:

#+begin_src fish
echo $fish_pid
command -q hello; echo $status
goblins request-package hello --reason "Try a live package grant"
#+end_src

=hello= is initially absent. Press =y= or =n= in the *serve terminal*.
There is no timeout for the human decision. On approval, run in the same Fish:

#+begin_src fish
hello
echo $fish_pid
#+end_src

The PID stays the same. The server realizes the pinned package on the host,
roots it, mounts every missing runtime dependency read-only into the existing
mount namespace, and only then switches the stable profile. The command returns
success only after that publication. =--reason= is optional.

Request packages by attribute name from the pinned nixpkgs revision, for example
=cowsay=, =tree=, or =python3Packages.black=. Names may have dot-separated
components; URLs, paths, Nix expressions, options, and output selectors are
rejected. Try =goblins request-package cowsay=, approve in the upper terminal,
then run =cowsay hello= in Fish. =jq= is already supplied by commonTools.
=script-tool= and =data-tool= are test fixtures for absolute store interpreters
and separate store data files. They are built and granted by the host test
driver, not exposed as package names through the public request command.

The host selects the =bin= output when present, otherwise the default output,
and mounts its runtime closure. This is executable provisioning, not a complete
development environment: additional development outputs and setup hooks are
not activated. Packages must exist and be buildable for x86_64-linux at the
pinned revision. Nixpkgs' default unfree/broken-package restrictions still apply.
Unknown attributes, non-package attributes, and build failures return =error=
(exit 1); the serve terminal shows the Nix diagnostic. Failure before mounting
leaves the shell running. A transport failure with an uncertain outcome remains
exit 2.

Type =exit= in Fish to stop that session; =serve= stays ready for another
=goblins run shell=. A new session does not inherit previous grants. This runtime
supports one attached shell at a time. The fullscreen table shows the connected
sandbox once, with host PID, state and startup/live package counts. Enter opens
the selected sandbox's package details. Press =q= / Ctrl-C to stop
the server and its shell.

* Fullscreen approval interface

=serve= enters the alternate screen and restores the terminal on exit. The
overview has one row per connected sandbox. Press Enter to open its scrollable
package table, distinguishing startup access from live grants; Escape returns
to the overview. A bordered request panel opens at the bottom
and reduces the table height; it never draws over table rows.

| Input | Action |
|-------+--------|
| =y= / =n= | Approve / deny the displayed request immediately |
| Enter (no request pending) | Open the selected sandbox's package details |
| Escape / Backspace | Return from details to the sandbox overview |
| Left / Right / Tab, then Enter (request pending) | Select a Yes/No button; No is selected initially |
| Mouse click | Activate the Yes or No button |
| Up / Down or =k= / =j=, Home / End | Scroll the package table |
| Mouse wheel | Scroll the table or request under the pointer |
| Page Up / Page Down | Scroll long request content; otherwise page through the table |
| =q= / Ctrl-C | Quit and stop the attached sandbox |

The panel shows package, sandbox, reason, host-store presence and a download
estimate. =Checking...= appears during the host query. A preview evaluates the
pinned package with import-from-derivation disabled and runs Nix =--dry-run=;
it can query caches and write evaluation metadata, but does not build the
requested output or grant sandbox access. Transfer estimates are Nix's
compressed missing-closure sizes, not unpacked sizes. Source builds can have
additional unknown costs, and unavailable metadata is shown as =Unknown=.
These values are advisory; approval is still required for realization/mounting.
Denial, request disconnect, attachment disconnect and quit cancel pending work.

Colors use the terminal's default foreground/background and ANSI palette.
Blue marks square borders/headings, green marks status/access and Yes, yellow
marks requests, and red marks No. The terminal theme supplies these ANSI colors;
Goblins does not sample the background or use a fallback RGB theme. Selection
uses reverse video (swapping foreground/background), and hints are dimmed.
The =NO_COLOR= environment convention is respected. Borders and text labels
also distinguish states without color. Very small terminals show a resize hint.
Bracketed paste is ignored for approvals, and request text is escaped before
rendering. The approval terminal remains separate from sandbox output.

=serve --plain= retains the line-oriented foreground interface for simple
terminals and diagnostics: =approve=, =deny=, =status= and =quit= followed by
Enter. Both frontends use the same controller and per-launch configuration flow.

* Boundaries

The host =goblins= executable implements =serve= and =run NAME=, with =shell= as
an alias for =run shell=. The variant mounted
inside the sandbox implements only =request-package=. Even arbitrary agent code
can only reach the per-session request socket at =/run/goblins/request.sock=;
it cannot reach the separate host control socket or the helper's private pipes.
The endpoint determines the session. Package requests cannot contain host paths,
PIDs, namespace descriptors, mount flags, Nix expressions, or approvals.

=serve= owns the Rust helper, session GC roots, and lifecycle. =shell= is a thin
host terminal attachment. It receives only its PTY master over the private host
control socket; the sandbox receives the matching slave as its own stdio and
controlling terminal. It never receives the serve terminal. Terminal dimensions
are forwarded without adding a general control RPC.

The connected shell uses the live-mount launcher extension described in
[[file:DESIGN.org][the design]], consuming the build specification produced by upstream
=mkSandbox=: executable, POSIX shell, PATH, full initial closure (including its
development outputs), certificate paths, and pkg-config paths.
The earlier unmodified upstream =fish-shell= wrapper remains a baseline only:
it lacks the retained mount helper and is not the connected =goblins shell=.
Unix sockets are deliberately enabled for the single mounted request endpoint;
private filesystem/network views hide ungranted host/session sockets. Explicit
startup directory binds expose their selected contents, including sockets there;
protected controller and Nix daemon paths cannot be granted.
Host Nix is never exposed. Effective and bounding capabilities are zero in the
connected payload, and seccomp denies namespace/mount authority.

See [[file:RESULTS.org][the result record]] for measured evidence and explicit limitations.

* Nix API

The root flake exports =lib.x86_64-linux.{mkGoblin,mkGoblins,commonTools,goblins}=. The
=packages.x86_64-linux.goblins= and default package are built with these helpers.
See [[file:README.org::*Configure named goblins with Nix][the configuration example]].

Use =lib.x86_64-linux.goblins.shell= for the predefined interactive Fish
configuration in =nix/goblins/shell.nix=. The host and inner binary definitions
live separately under =nix/packages/=. The preset uses commonTools and is the
default =goblins run shell= configuration; custom entries still use =mkGoblin=.

=mkGoblins { goblins = { NAME = mkGoblin { ... }; }; }= returns a Nix derivation
providing the host =bin/goblins= executable, with =config= and =goblins=
attributes for inspection. Names select entries through =goblins run NAME=.
The host =run NAME= client sends its manifest's absolute path and the selected
name over the private attachment socket. The server loads that manifest for
each new launch. Rebuild your Goblins package and run its new executable: the
existing server can launch changed definitions and newly added names without
restarting. The names printed when =serve= starts describe its own build only.

Exit the active goblin before launching the updated one; one session at a time
is still supported. Existing sessions keep their selected launch configuration.
Package approval and workspace selection remain with =serve=. Configuration
paths are accepted only from host attachments, never sandbox package requests.
Malformed, missing, oversized and non-regular manifests return launch errors.

A server running code from before this feature needs one restart to install the
updated controller. Subsequent configuration-only rebuilds need no server
restart. This does not add controller restart recovery.

=mkGoblin= calls upstream =mkSandbox= with the inner request executable added
to =allowedPackages=. Its result carries upstream's =buildSpec= plus a =goblin=
configuration consumed by =mkGoblins=. Launch it with =goblins run= to receive
the session request socket and retained mount helper. Directly invoking the
upstream wrapper is a separate static sandbox and has no approval connection.

| Option | Meaning / default |
|--------+-------------------|
| =pkg= | Required Nix package providing the entry executable |
| =binName= | Required executable name under =pkg/bin= |
| =outName= | Upstream wrapper name; defaults to =goblin-${binName}= |
| =allowedPackages= | Initially available packages; defaults to upstream =commonTools= |
| =args= | Literal entry argument list; defaults to =[]= |
| =env= | Literal string environment values; defaults to ={}= |
| =allowNix= | Must be =false=; host daemon stays hidden |
| =allowUnixSockets= | Must be =true= for the request transport |
| =allowedDomains=, =allowedHostPorts=, =publishedPorts= | Currently must be =[]= |
| =rwDirs=, =rwFiles=, =roDirs=, =roFiles= | Forwarded startup host bind paths; defaults to =[]= |

Unsupported network settings and malformed option types fail at Nix evaluation. Unlike upstream's
environment fragments, Goblin =env= values are literal: =$HOME= and =$(...)=
are not expanded or executed on the host. =PATH=, =HOME=, =SHELL=, =USER=,
=LOGNAME=, and =PYTHONNOUSERSITE= are reserved. Store paths used in =env= must
also be included through an allowed package's closure to be visible inside.

The four bind options use the upstream names and string-list format. Paths
expand =$VAR=, =${VAR}= and leading =~/= using the host =serve= environment;
no shell command is evaluated. Bind sources must exist, match the declared
file/directory kind, and have absolute paths without =..=. The private sandbox
HOME uses the host HOME pathname, so a =$HOME/.config/fish= bind is also where
Fish looks. The rest of HOME remains private. Writable binds modify host data;
read-only binds reflect host changes without allowing sandbox writes.

Goblins rejects overlapping bind destinations and binds covering protected
namespace, store, workspace or controller paths. Symlinks in selected directories
do not automatically grant other host files. Links into the immutable Nix store
expose only their selected targets, including links inside config subdirectories;
those store outputs are rooted without mounting their entire closures. Symlinks
added or retargeted to previously hidden store targets require a new session.
The directory itself is bound, never copied. Startup binds do not add a live
file-grant operation or change the disposable =--workspace= snapshot behavior.

The initial closure now comes from =mkSandbox=, including its implicit
packages and development outputs. Later package requests still select =bin=
or the default output and its runtime closure; they do not activate development
setup hooks. =/bin/sh= uses upstream's Bash wrapper even when Fish or another
program is the goblin entry point.

* Workspaces and state

By default each shell starts in an empty disposable =/workspace=. To use a
repository snapshot, configure it on the host server:

#+begin_src sh
goblins serve --workspace /path/to/repository
#+end_src

The host copies regular files, directories, and symlinks before launch. It
follows no source symlinks, skips all =.git= entries, and omits sockets, FIFOs,
and devices. There is no shared Git metadata or shared host workspace mount.
No hooks or project configuration are executed on the host. Other regular files
are copied, so select an appropriate source. Concurrent host edits can yield
an inconsistent snapshot or a copy error. The copy is deleted on session exit;
artifact export is not implemented.

The private host control socket is normally under =$XDG_RUNTIME_DIR/goblins/=
(or =/run/user/UID/goblins/=, with a private =/tmp/= fallback). An exclusive lock
prevents a second server from taking over its socket. For isolated test instances,
pass =--state-dir PATH= before =serve= and =shell= on both host commands. This
path is never mounted in the sandbox.

Session logs, request socket, mountpoint placeholders, profiles, workspace, and
GC roots live in a private mode-0700 =/tmp/goblins-*= directory. Normal exit
removes it. Abrupt server death kills the sandbox through parent-death handling
but can leave its directory and roots behind. After verifying that specific
session is gone, remove its directory manually. No cleanup service or global
Nix garbage collection is installed. Logs are currently disposable with the
session; durable records are deferred; controller restart recovery is explicitly out of scope.

* Reproducible checks

The connected test creates two real terminal processes, runs =serve= and
=shell=, denies and then approves =hello= through the server terminal, and checks
that Fish's PID/start time/mount namespace are unchanged. It also exercises
terminal interruption/resize, the restricted in-sandbox executable, hidden host
sockets, session isolation, and shutdown.

#+begin_src sh
nix develop path:.
cargo test --workspace --locked
python3 -m unittest discover -s tests -p 'test_*.py'
nix flake check path:.
#+end_src

The six original namespace scenarios now use a test-only Rust Session driver.
The Python harness builds that driver with Cargo, transfers the payload pipes
and listener using SCM_RIGHTS, and runs the existing payload probes in the real
sandbox. Set =GOBLINS_TEST_DRIVER= to a prebuilt Rust test executable if needed.
Fault injection exists only in that test executable. The connected and
responsiveness suites exercise the packaged production CLI directly through
=serve --plain=. The fullscreen suite uses pyte (supplied by =nix develop=) to
check actual rendered screens, colors, input and terminal restoration.

For Rust development:

#+begin_src sh
nix develop path:.
cargo build --workspace --locked
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo fmt --all --check
#+end_src

The Nix Rust builds also run their unit tests. Host-driven integration checks need
host Nix and Unix socket access; an enclosing Codex sandbox may require its
normal approval mechanism. Tests must not weaken the PoC boundary to bypass
an outer sandbox denial.

* Requirements and current limits

Linux x86_64 with unprivileged user namespaces, a non-root host user, Nix flakes,
and support for =openat2=, =open_tree=, =mount_setattr=, =move_mount=,
=close_range=, and namespace ownership ioctls. The Nix output supplies the pinned
runtime. Tested on NixOS/Linux 7.2.4. =serve= and =shell= require host terminals.

This is a package-only proof, with no real agent credentials or authentication.
There are no CPU/memory/process/disk quotas, concurrent shell management,
durable retries, controller restart recovery, or live revocation. Runtime closures are
readable/executable; arbitrary code execution is expected. Procfs can expose
kernel/hardware information. Namespace creation is blocked in the payload.

Grants are additive and individual paths become visible before profile
publication. Mount/publication failure kills the disposable session; it cannot
erase data already read. Nix runtime references are honored, but setup hooks,
compiler environments, services, or unrecorded plugins/data are not configured
automatically. This is not a production sandbox security certification.

Incomplete public requests expire after three seconds total, even if they drip
bytes. Approval/build time is separate and has no automatic approval or retry.
One package request can be awaiting a decision or grant at once; extra complete
requests receive a correlated busy error. The table and quit key remain responsive
during incomplete requests, pending approval, previews, startup and builds.
The plain frontend also keeps =status= and =quit= available. Closing the
host attachment stops its session in all these states.

Stopping a build terminates and reaps the owned Nix client; it does not kill the
host Nix daemon or guarantee cancellation of daemon work shared with others.
Controller restart never resumes a prior session. There is no daemon, Emacs
integration, file/network live grant or credential integration here.
