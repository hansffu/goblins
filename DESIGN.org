#+title: Live grant boundary and implementation

* Connected command interface

=goblins serve= owns the session and helper. =goblins shell= connects to a
separate, private host-only SOCK_SEQPACKET control socket and requests one shell
with its terminal dimensions. The session worker creates a PTY, starts the configured entry on its slave,
and transfers the master to the host attachment using SCM_RIGHTS. The shell
client relays bytes and window-size changes, restoring its terminal on exit.
This provides a real terminal without adding terminal multiplexing or persistence.

The helper attaches the newly allocated slave as the payload's controlling
terminal before Bubblewrap setup. It closes every other handle on exec. The
sandbox's =/dev/tty= now intentionally names its own shell terminal, never the
serve terminal. Pipe-based gate tests still have no controlling terminal.

The host control socket and the per-session public request socket are different
interfaces. Only the public request socket is mounted into Fish. Host socket
messages select one named host configuration, with bounded terminal dimensions;
there is no approval or arbitrary mount operation on that protocol. The server's
foreground terminal remains the only source of package approval. The mounted
=goblins= executable implements only =request-package=, and is not the host CLI.

The server keeps an exclusive lock on a private mode-0700 control directory.
The control connection's lifetime owns the attachment: disconnect stops the
session; server exit stops its helper and restores the shell terminal. One
shell is supported at a time, with sequential fresh sessions on the same server.
The source, target namespace, closure rooting, and publication mechanisms below
are shared with the original gate.

* Fullscreen presentation and package previews

The fullscreen frontend uses Ratatui for table/layout rendering and Crossterm
for terminal events. These dependencies are confined to the host controller
package; the inner request client and namespace helper do not depend on them.
The controller library still never opens =/dev/tty=. The frontend owns alternate
screen/raw mode, resize, scroll state, colors and input. Its request panel reserves
vertical space below the table rather than overlaying it. Terminal ANSI colors
are inherited by default; OneDark is an explicit alternative.

A validated package request emits an approval token immediately and queues a
preview on the existing session worker. Preview evaluation disables IFD and
uses the selected configuration's pinned nixpkgs. For an absent output, Nix's
C-locale raw =--dry-run= summary supplies the compressed download estimate;
its build JSON does not contain transfer sizes. Unexpected summary formats,
lookup failures and unavailable metadata yield Unknown. Source builds are
marked as having unknown total cost. The normal approved build retains its
existing realization behavior.

Each preview has a cancellation token linked to session cancellation. Denial
or request disconnect cancels only the preview; session shutdown cancels both.
No preview can publish a profile, mount a package, or authorize a grant. The
worker remains sequential, separate from the event pump and namespace helper.
Preview and decision events carry the controller-issued approval identity;
another sandbox client's duplicate public request ID cannot replace or dismiss
the displayed decision. Bracketed paste cannot supply approval keys.

* Namespace topology

#+begin_example
host controller (ordinary host UID; Nix access; foreground lifetime)
  private pipes -> Rust helper
    user namespace A: UID 0 maps only to the invoking host UID
    mount namespace S owned by A: private copy of source mounts
    final helper capabilities: CAP_SYS_ADMIN | CAP_SYS_CHROOT in A
      Bubblewrap monitor
        user namespace B (child of A)
        mount namespace M owned by B
        private PID / network / IPC / UTS / cgroup namespaces
          PID 1: Bubblewrap reaper
          PID 2: original Fish (Bash in the gate), zero capabilities, no_new_privs
            descendants inherit the same restrictions
#+end_example

There is no host-root process. The Rust helper refuses host UID 0, creates A,
maps only the invoking UID/GID, denies setgroups, and creates S. It marks S's
mount propagation recursively private. S retains trusted source access; the
payload does not inherit S's descriptors or root.

Bubblewrap creates B and M with =--unshare-all --cap-drop ALL=. Its private
=--info-fd= reports the child PID. Its =--block-fd= holds the payload until the
helper observes the final root and verifies namespace ownership. The helper
uses =NS_GET_USERNS= and =NS_GET_PARENT= to verify that M's owner is a direct
child of A, and independently verifies S's owner is A. It never accepts a PID
or namespace from the agent.

The helper stays outside the sandbox PID namespace, is nondumpable, and drops
all capabilities except =CAP_SYS_ADMIN= and =CAP_SYS_CHROOT= after setup. Its
authority applies to A and descendant namespaces, not the host user namespace.
Both its source and target mount namespaces are per-session namespaces. The
agent has no namespace descriptors or mount-control pipe. Its only inherited
descriptors are payload stdio; Bubblewrap's reaper also holds its eventfd.

* Why retaining source and target namespaces matters

The first kernel trial successfully entered M but failed =open_tree= with
=EINVAL=: the source descriptor referred to a mount outside the helper's
current mount namespace. This is a concrete failure of that first mechanism,
not a blocker for live mounts. The working helper switches to S to clone the
source, hardens the detached mount, switches to M, and attaches it. It retains
no descriptor for entering the host's mount namespace.

Each grant operation:

1. The host controller validates a nixpkgs attribute name and obtains its pinned
   output from host Nix. It creates GC roots before enumerating requisites.
2. The controller enumerates the transitive closure through host Nix, validates
   canonical store names, checks executable-name collisions, and prepares a
   profile generation. It creates empty destination placeholders in a
   host-owned directory already bound read-only at =/nix/store=.
3. The private helper protocol carries only one bounded canonical store
   basename per operation. No source prefix, destination, PID, or flags are
   accepted. Agent requests never reach this protocol directly.
4. In S, the helper opens the source relative to its retained store descriptor.
   =openat2= uses =RESOLVE_BENEATH | RESOLVE_NO_SYMLINKS |
   RESOLVE_NO_MAGICLINKS=. Only regular files and directories are supported.
5. It resolves the destination relative to the retained sandbox root with the
   same lookup restrictions. The agent cannot modify the read-only parent
   hierarchy or create placeholders; fault injection also tests rejection of
   a host-inserted destination symlink.
6. =open_tree(OPEN_TREE_CLONE)= clones the single source object into a detached
   mount. =mount_setattr= sets =RDONLY | NOSUID | NODEV= *before* visibility.
   No recursive import of host submounts is performed.
7. In M, =move_mount= attaches the hardened clone by source/destination FDs.
   The helper acknowledges success; errors exit and kill the disposable tree.
8. Only after every acknowledgement does the controller atomically rename the
   =current= symlink under the read-only mounted profile parent.

The empty host =store/= directory contains mountpoint placeholders, not copied
package contents. Absolute =/nix/store/= identities and package symlinks remain
intact. A grant is not an atomic set of mounts; direct paths may be readable
before =current= changes. Partial failure ends the session, not a rollback.

* Payload policy and socket exception

The launch specification follows upstream's selective read-only closure mounts,
private HOME/tmp, and Bubblewrap boundary. Goblins uses an explicit adapter
because the upstream wrapper has no retained live-mount helper/control channel.
The pinned upstream wrapper is separately evaluable/buildable as
=upstream-baseline=; it is not the executable used for the live experiment.

The upstream default denies =socket(AF_UNIX)=. This adapter intentionally allows
Unix sockets so the one pathname request socket works. It mounts the socket
itself at =/run/goblins/request.sock=, not its parent directory. Other session
sockets, the Nix daemon, and socket inodes in workspace snapshots are absent.
Explicit startup host directory grants expose their contents, including any
socket inodes there; the launcher refuses grants covering its private control
and session directories. A separate network namespace isolates abstract Unix
sockets and host IP services. There is no pasta forwarding or host networking.

An x86_64 seccomp filter rejects mount APIs, =setns=, =unshare=, namespace-creating
=clone= flags, ptrace, and open-by-handle. It rejects alternate syscall ABIs.
=clone3= returns =ENOSYS= to permit libc's ordinary-process =clone= fallback.
Normal child execution succeeds; all capabilities and the capability bounding
set remain zero in the payload. This supplements filesystem, PID, and network
isolation; it is not a comprehensive syscall allowlist.

* Controller, transport, and terminal separation

The Nix library builds named configurations. =mkGoblin= calls the pinned
=mkSandbox= with the inner request package added, and retains its build spec.
=mkGoblins= serializes the configurations into an immutable host-side manifest
and returns =bin/goblins=. The live adapter reads upstream's entry executable,
POSIX shell, full initial closure, PATH, certificate and pkg-config paths. It
does not run upstream's independent static launcher or its environment shell
fragments. Configured environment values and entry arguments remain literal.
The four upstream host bind lists are forwarded and implemented by the Rust
startup adapter. Types and unsupported network options are checked at Nix
evaluation; source existence, types and protected-path conflicts are checked at
launch. See [[file:USAGE.org::*Nix API][bind semantics and limits]]. HOME retains its host pathname
but is a private tmpfs with only declared paths bound into it.

The adapter discovers store symlinks within declared directories using
no-follow directory descriptors. It roots their containing store outputs while
mounting only the exact selected targets; this supports Home Manager without
exposing unrelated settings or recursively granting a config's dependencies.
Startup host bind sources are trusted host configuration. Concurrent host
replacement of source paths during launch is not covered by the live store
helper's openat2 guarantee.

The host =run NAME= client sends a configured name and an absolute manifest path
over the private control socket. This is a trusted host interface: selecting a
manifest selects executable paths and startup policy. The session worker reads
and selects the configuration for each launch; the server has no fixed registry
or manifest-identity equality check. Loading stays outside the event pump.
Manifest reads are bounded to 1 MiB and require regular files; FIFO/device paths
cannot block startup. Invalid manifests and unknown names fail that launch.

Changed definitions and new names can therefore run on the same server without
restart. Active sessions retain their launch configuration, and a second
attachment is rejected without replacing them. =shell= is a CLI alias for
=run shell=. Only =request-package= is available in the inner command: its
protocol cannot select manifests, executables, host paths or approval decisions.
The server still owns workspace snapshots, grant approval and session teardown.
This per-launch configuration selection provides no restart recovery.

The Rust workspace has three authority boundaries:

- =goblins-protocol= defines public data, strict JSON validation and framing.
- =goblins-client= depends on that protocol and JSON serialization only. Its
  executable is installed as =bin/goblins= in the inner Nix package (with the
  historical =goblins-request package NAME= alias).
- =goblins-controller= owns host policy and session lifecycle. Its library
  contains configuration adaptation, the event pump, session worker, Nix
  commands, snapshots and launch/mount coordination. Its =goblins= executable
  supplies the fullscreen approval UI, plain fallback and host terminal relay.

The separate =goblins-mount-helper= retains the PoC implementation and depends
only on =libc=. No controller thread performs namespace operations. The helper
is single-threaded, with its own source/target namespace and control descriptors.
The only third-party runtime dependencies are =libc= and =serde=/=serde_json=
(with their locked transitive dependencies). There is no async runtime or CLI
framework. Descriptor ownership uses =OwnedFd=/=File= and owned processes are
reaped on shutdown/error paths.

The Nix configuration model is unchanged. =mkGoblin= still wraps =mkSandbox=;
=mkGoblins= projects its build specification for the live adapter, omitting
unused references to the upstream static Python/network launcher. Initial
closures, allowed packages, entry arguments and literal environment values are
retained. The original upstream wrapper remains available through =mkGoblin=.
Neither Rust executable needs Python. A configured package may itself depend
on Python, and the integration probes deliberately include it.

Protocol: one UTF-8 JSON line, at most 4096 bytes, with exactly:

#+begin_src json
{"v":1,"id":"r1","op":"request-package","package":"jq","reason":"Inspect test output"}
#+end_src

IDs use 1–64 ASCII letters/digits/underscores/hyphens. Reasons contain 1–1024
characters, subject to the wire limit. Duplicate/unknown fields, invalid types,
unsupported operations, and malformed/deep JSON are errors. A syntactically
valid unknown package reaches approval and then gets a correlated Nix resolution
error. Framing has a monotonic *whole-request* deadline of three seconds from
acceptance; receiving more bytes never resets it.

The frontend calls =Controller::tick= and renders returned events, passes an
opaque controller-issued =ApprovalId= to =decide=, and can inspect or stop a
session. The approval token is independent of the agent-chosen correlation ID.
The controller library never reads =/dev/tty=. No host socket operation can
approve a grant; the foreground frontend supplies that explicit decision.

A small event pump accepts and reads sockets nonblockingly, limiting each kind
to 16 incomplete connections and bounded work per tick. One complete request
may await approval or grant processing; other complete requests receive a
correlated busy error. Slow readers never receive an unbounded response buffer.
The host attachment handshake is also bounded and has a whole-packet deadline.
There is no durable queue, scheduler, or concurrency between grants.

One persistent worker thread owns the session and handles startup, snapshots,
Nix and helper replies. The event pump continues during these operations and
while awaiting a human. Attachment disconnect sets a cancellation token without
waiting for the worker's command queue. The foreground UI polls at 20 ms;
worker waits and helper replies check cancellation at similar intervals. These
are polling intervals, not hard real-time latency guarantees.

On cancellation, an owned host command's process group is killed and its direct
child reaped. =waitid(WNOWAIT)= keeps the child's PID reserved until group
termination, avoiding a signal to a recycled PID. The host Nix daemon is not
owned or killed; cancellation of its client is not a claim that shared daemon
build work stops. Helper death tears down the sandbox. Local filesystem I/O and
kernel calls are not made cancellable, so an unresponsive filesystem/kernel can
still delay cleanup. No reconnect, saved state or restart recovery is provided.

A valid request yields =ready=, =denied=, or =error= with matching version/ID.
The CLI succeeds only on =ready=. An interrupted/missing/invalid reply yields
an explicit unknown outcome and exit 2, without resubmission. Disconnect alone
never constitutes approval. A human-approved grant may still complete after a
client disconnects, leaving that client uncertain about its outcome.

The foreground client opens =/dev/tty= directly for decisions. The connected
payload has its own PTY slave; only the host attachment client gets its master.
The host-library integration tests use pipes. Neither receives the approval terminal. Request
fields and responses are escaped before rendering in the host approval UI;
payload terminal output goes directly to its separate attachment terminal.
No agent-generated text is fed into the approval input. The host controller
API used by tests is trusted; it is not exported on the public socket. A test-only
Rust harness transfers payload pipe/listener descriptors to Python probes.
Fault injection is compiled only into this test harness, never installed as a
production command or accepted in a manifest or socket request.

* Lifetime and failure policy

Host Nix resolves validated attribute components directly from the pinned
nixpkgs source's =legacyPackages.x86_64-linux= package set. The host executable
references =pkgs.path= from the locked input; it does not embed the Goblins root
flake, tests, or fixture definitions. Test drivers build fixtures separately
and pass their already-realized outputs to the host-side grant API.
A fixed host-side Nix function requires a derivation and chooses its =bin=
output, or its default output when no =bin= exists. The host supplies the build
output selector; the request cannot. Evaluation and realization happen only
after human approval. No agent workspace files, supplied expressions, URLs,
or package strings as shell code are evaluated. The package grammar is bounded
to 200 ASCII characters with dot-separated identifier components.
Top-level roots retain transitive dependencies for the session. The profile
contains absolute links to approved =bin= entries, with collision rejection.
There is no shell restart or environment replacement; the stable profile path
was on PATH from launch.

Realization or pre-mount collision errors leave the running session/profile
unchanged. Once mounting/publication begins, any failure ends the session.
Helper termination kills Bubblewrap; Bubblewrap parent-death handling and PID
namespace teardown kill the payload tree. Controller exit stops the helper.
SIGKILL cannot clean filesystem state, so crash leftovers require host cleanup.
No global garbage collection is run by this PoC.

Controller/daemon restart recovery is explicitly out of scope. Normal frontend
shutdown joins the worker and removes session roots/directories. SIGKILL cannot
perform that filesystem cleanup. A future daemon or Emacs frontend can host the same
controller operations, but neither service lifecycle nor surviving a controller
restart is implemented by this rewrite.
